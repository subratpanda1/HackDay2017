<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SettingsSection">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:background">@drawable/view_alpha_selector</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="SettingsDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/divider_color</item>
    </style>

    <style name="cardStyle">
        <item name="android:gravity">center</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginTop">@dimen/medium_dimen</item>
        <item name="cardElevation">@dimen/tiny_dimen</item>
        <item name="contentPadding">@dimen/medium_dimen</item>
        <item name="cardCornerRadius">5dp</item>
    </style>

    <style name="microphoneStyle">
        <item name="mainColor">@color/mic_colors</item>
        <item name="maxRadius">@dimen/mic_button_halo_radius_max</item>
        <item name="minRadius">@dimen/mic_button_halo_radius_min</item>
        <item name="centerColor">@color/icon_orange_color</item>
        <item name="haloColor">@color/mic_button_halo</item>
        <item name="android:src">@drawable/mic_control</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="android:layout_margin">0dp</item>
        <item name="android:layout_width">@dimen/fab_margin</item>
        <item name="android:layout_height">@dimen/fab_margin</item>
        <item name="android:scaleType">centerInside</item>
    </style>

</resources>
